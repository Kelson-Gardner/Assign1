/**
 * @author Kelson Gardner
 * Interface for strategy pattern
 */
public interface ActionStrategy {
    /**
     * Interface method for strategy pattern
     * @param input input from the user
     */
    void execute(String input);
}
